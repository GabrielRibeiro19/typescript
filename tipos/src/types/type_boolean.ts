// Type BOOLEAN

let isAuthenticate: boolean = true

let codeStatus = ""

// Tudo que for diferente de zero, string vazia, undefined, será verdadeiro.
isAuthenticate = Boolean(codeStatus)


console.log(isAuthenticate)
